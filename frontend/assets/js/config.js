// ---------------------------------------------------------------------------
// Backend connection settings.
// Change API_BASE if your FastAPI server is running somewhere other than
// http://127.0.0.1:8000 (e.g. a deployed URL).
// ---------------------------------------------------------------------------
window.APP_CONFIG = {
  API_BASE: "http://127.0.0.1:8000",
};
